# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Local demo database + secrets
/data
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bun lockfile. This project builds with npm (package-lock.json is the
# real lockfile). A stale bun.lock made Railway pick Bun and fail on
# --frozen-lockfile, so keep it out of the repo entirely.
bun.lock
bun.lockb
